(jbuild_version 1)

(library (
  (name capTP)
  (libraries (lwt.unix cohttp.lwt astring capnp))
))

(rule
 ((targets (schema.ml schema.mli))
  (deps (schema.capnp))
  (action  (run capnpc -o ocaml ${<}))))
